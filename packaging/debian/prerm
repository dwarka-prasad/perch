#!/bin/sh
set -e
# Stop a running per-user service if present (best effort; ignore failures).
if command -v systemctl >/dev/null 2>&1; then
    systemctl --user stop perch 2>/dev/null || true
fi
exit 0
